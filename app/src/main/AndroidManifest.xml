<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pazaryeri">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".animasyon_page"></activity>
        <activity android:name=".Giris" />
        <activity android:name=".eski_siparis_Ayrinti" />
        <activity android:name=".Kisisel_Bilgi" />
        <activity android:name=".Eskisiparis" />
        <activity android:name=".Sifre_degistir" />
        <activity android:name=".Kayit_ol" />
        <activity android:name=".Siparis_sayfasi" />
        <activity android:name=".Urunler" />
        <activity android:name=".Kisisel_bilgiler" />
        <activity android:name=".Anasayfa" />
        <activity android:name=".tanitim.first">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.parse.SERVER_URL"
            android:value="@string/back4app_server_url" />
        <meta-data
            android:name="com.parse.APPLICATION_ID"
            android:value="@string/back4app_app_id" />
        <meta-data
            android:name="com.parse.CLIENT_KEY"
            android:value="@string/back4app_client_key" />
    </application>

</manifest>